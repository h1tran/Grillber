import React from 'react';
import Directory from '../../Directory'

const Homepage = props => {
    return (
        <section>
            <Directory />
        </section>
    );
};

export default Homepage;